# ひらがな麻雀 仕様書

参考: ひらがな麻雀「ひらがじゃん 牌ばーじょん」ルール紹介動画 (https://youtu.be/1YdwN1sekdU)
※本アプリは同ゲームにインスパイアされた非公式のファンメイド実装。名称・ロゴ等の公式ブランドは使用しない。

この文書は **3層に分けて** 書く。層をまたぐ変更は、必ず該当層の記述を同時に更新すること。

| 層 | 内容 | 実装 | 動作環境 |
|---|---|---|---|
| **1. コアルール** | 牌・配牌・進行・鳴き・和了判定・得点 | `src/engine.js`, `src/ai.js` | 単体で動く純ロジック(DOM・通信なし) |
| **2. クライアント** | 画面遷移・演出・入力・ローカル保存 | `index.html`, `css/`, `src/ui.js` ほか | 静的Web (サーバ不要) |
| **3. サーバ** | オンライン対戦の権威・部屋・マッチング | `tools/gameserver.mjs` | Node + WebSocket が必要 |

**重要**: 層2までは静的ファイルのみで完結し、オフラインでも「フリー対戦(対CPU)」が遊べる。
**ランク戦・ともだち対戦は層3が必須**であり、静的ホスティング単体では動作しない。
詳細なサーバ仕様は [docs/SERVER_SPEC.md](docs/SERVER_SPEC.md) を参照。

---

# 層1: コアルール仕様

## 牌 (全120枚)
| 種別 | 種類数 | 1種あたり | 小計 |
|---|---|---|---|
| 清音 | 45 (あ〜ん、「を」は無し) | 2枚 | 90 |
| 濁音 | 20 (が〜ぼ) | 1枚 | 20 |
| 半濁音 | 5 (ぱ〜ぽ) | 1枚 | 5 |
| 小文字 | 4 (ゃゅょっ) | 1枚 | 4 |
| 長音 | 1 (ー) | 1枚 | 1 |

**レア牌** = 1枚しかない牌(濁音・半濁音・小文字・長音の計30枚)。得点加算の対象。
小さい「ぁぃぅぇぉゎ」および「を」は**牌として存在しない**。

## 進行
1. 各自13枚を配る。残りが山 (4人打ちなら68枚)。
2. 手番: 山から1枚ツモる → 以下のいずれか。
   - ツモ和了を宣言する (成立時のみ)
   - カンを宣言する (成立時のみ。宣言後は再び同じ選択に戻る)
   - 手牌から1枚捨てる
3. 捨て牌に対し他家がロン/ポンを宣言できる。誰も宣言しなければ次の手番へ。
4. 山が尽きた時点で流局。

## 和了 (あがり)
手牌13枚 + ツモ/ロン牌1枚 = 14枚が
**「2文字の語×1 + 3文字の語×4」の計5語**で構成されたら和了。

### 並べてあがる方式 (2026-08-16 ルール変更・ユーザー決定)
**人間のあがりは自動検出しない。** 手牌の表示順が左から語のブロックに区切れているときだけ成立する。
- ツモ: 表示順が `checkArrangedWin` を満たすときだけ「ツモ!」ボタンが出る。2文字語のブロックは先頭・中間・末尾どこでもよい。
- ロン: 13枚の並びのどこか1箇所に捨て牌を差し込めば完成する形 (`checkArrangedRon`) に並べてあるときだけ宣言できる。
- CPUは従来通り辞書探索 (`findWin`) であがる。CPUの強さは語彙数で制御。
- オンラインでは並びをクライアントが `arrange` メッセージで随時送信し、サーバはあがり宣言時に
  act 添付の並びをマルチセット検証してから再判定する (サーバ権威は維持)。
- 背景: 辞書全網羅 (16,788語) では配牌テンパイ率66%となり自動検出では1〜2巡で決着してしまう。
  並べ方式なら辞書は全網羅のまま、プレイヤーの「ことばを見つけて並べる力」がゲームの腕前になる。

鳴き1回につき手牌の必要枚数が3枚減る:
- 鳴き0回 → 手牌14枚を「2文字×1 + 3文字×4」
- 鳴き1回 → 手牌11枚を「2文字×1 + 3文字×3」(+晒し1語)
- 鳴き2回 → 手牌8枚、鳴き3回 → 手牌5枚

### 明示的な裁定 (実装が依存する決定事項)
| 論点 | 裁定 | 根拠 |
|---|---|---|
| 同じ語を複数回使えるか | **許可**。手牌内で同一語を2回使う和了も有効 (例:「みかん」×2) | 牌の枚数制約で自然に上限がかかるため。`test_engine.mjs`「同じ語を2回使う上がりも可」 |
| 晒し語と手牌内の語の重複 | **許可**。制限しない | 同上 |
| 複数人が同時にロンできる場合 | **頭ハネ**。捨てた人から反時計回りで最初の1人だけが和了 | `claimsPhase` が順に判定し最初の成立で確定 |
| ロンとポンの競合 | **ロンが優先** | 全員のロン判定を先に回してからポン判定 |
| 2文字語が作れない14枚 | 和了ではない (必ず2文字語1つが必要) | `findWin` は2文字語を固定してから3文字語で被覆 |
| 鳴きの上限 | 3回まで (手牌に最低「2文字+3文字」を残す) | `canKan`/`canPon` が `melds.length >= 3` で拒否 |
| カン後の補充 | 山の**末尾**から1枚。山が空ならその場で流局 | `declareKan` |
| 流局時の点数 | 移動なし | `endRoundDraw` |
| 同点時の順位 | 座席順(インデックス昇順)が上位 | 順位は安定ソートで決定 |

## 鳴き
- **ポン**: 他家の捨て牌1枚 + 自分の手牌2枚で**3文字語**が完成する場合に宣言。晒して確定し、続けて1枚捨てる(ツモはしない)。
- **カン**: 自分の手牌のみで**4文字語**が完成している場合に自分の手番で宣言。晒して山の末尾から1枚補充。4文字語は3文字語1枠として数える。
- **ロン**: 他家の捨て牌で和了形が完成する場合に宣言。

## 得点
```
得点 = 8 (基本) + 2 × (和了形に含まれるレア牌の枚数) + 2 × (カンの回数)
```
- ツモ: 他家全員が `ceil(得点 ÷ (人数-1))` ずつ支払う。
- ロン: 放銃者が全額支払う。
- 規定局数(既定2局または4局)終了時の合計点で順位を決める。

## CPU思考 (`src/ai.js`)
- 手牌評価 = 完成語(100点) + あと1枚の語(45点) + あと2枚の語(15点) の貪欲加算。
- 捨て牌は「捨てた後の評価が最大」になる牌を選ぶ。
- **難易度は語彙率で表現する**: CPUには全辞書の一部だけを与える (`subsetDict`)。
  知識量の差として表れるため、思考力を落とす方式より自然。

---

# 層2: クライアント仕様

## 画面
| 画面 | 役割 | サーバ必要 |
|---|---|---|
| タイトル | モード選択。**初回は「はじめる/ルール」中心に簡素化**し、2回目以降に全機能を出す | 不要 |
| フリー対戦設定 | 人数(2〜4)・CPUの強さ・局数 | 不要 |
| ランク戦 | ランク表示とマッチング | **必要** |
| ともだち対戦 / ロビー | ルームコードの発行・参加 | **必要** |
| 対局 | 卓・手牌・ガイド・演出 | モードによる |
| ことば図鑑 / 実績 / ショップ / ルール | 収集要素と説明 | 不要 |

## 対局画面の原則
- 縦持ちスマホ最優先。最小幅320pxで手牌14枚とアクションバーが破綻しないこと。
- **今やることを常に1行で示す** (`#guide-bar`)。手番・鳴き可能・和了可能で色と文言が変わる。
- ツモってきた牌は右端に分離し、金枠と「ツモ」ラベルで明示する。
- 対局中に和了候補の単語は表示しない(ヒント券使用時のみ表示。フリー対戦限定)。

## ローカル保存 (localStorage)
| キー | 内容 |
|---|---|
| `hiragana_mahjong_rank_v1` | ランク・RP・戦績 |
| `hiragana_mahjong_economy_v1` | コイン・所持テーマ・ヒント券・広告なし |
| `hiragana_mahjong_collection_v1` | ことば図鑑 |
| `hiragana_mahjong_achievements_v1` | 実績と統計 |
| `hiragana_mahjong_seen_intro` / `_muted` / `_name` | 初回説明済み / ミュート / 表示名 |

保存はすべて **best-effort**。プライベートモード等で失敗しても例外を投げず進行を止めない。

## オフライン時の挙動
- 起動時に `GET api/online` を叩き、応答があるときだけオンライン系メニューを出す。
- 応答がない場合: 「ともだち対戦」は非表示、「ランク戦」は押すとサーバ版への案内を表示。

## アクセシビリティ方針
- アイコンのみのボタンには `aria-label` を必ず付ける。
- すべての入力に `<label for>` を対応させる。
- `:focus-visible` の輪郭を消さない(金色2pxで明示)。
- `user-select: none` / `touch-action: none` は**牌と操作UIに限定**し、文章領域には掛けない。

---

# 層3: サーバ仕様

→ [docs/SERVER_SPEC.md](docs/SERVER_SPEC.md) に分離。イベント一覧・状態遷移・タイムアウト・切断時の扱い・既知の制約を記載。

---

# 辞書仕様

## 採用基準 (機械判定できる形)
語 `w` は次を**すべて**満たすとき採用する。
1. `[...w].length` が 2 / 3 / 4 のいずれか (小文字「ゃゅょっ」と「ー」も1文字と数える)。
2. 使用文字がすべて牌に存在する (清音45 + 濁音20 + 半濁音5 + 小文字4 + 長音1)。**「を」を含む語は不可**。
3. 牌の枚数制約を満たす: 同一の清音は最大2回、レア牌(濁音・半濁音・小文字・長音)は最大1回まで。
4. 品詞は名詞・動詞(終止形のみ)・形容詞(終止形のみ)・日常的な副詞。活用形は収録しない。
5. 固有名詞はホワイトリスト方式(既定では不採用)。
6. 重複なし。バケットごとに五十音順にソート。

判定は `tools/validate_words.mjs` が機械的に検証し、CI相当としてテスト実行時に走らせる。
1〜3および6は完全に自動判定できる。4〜5は語彙リスト作成時の人手基準であり、
`tools/wordlist*.txt` の各ファイル冒頭にどの基準で集めたかを記す。

## 採用範囲 (2026-08-15 方針変更: 全網羅)
**ユーザー決定により、辞書は全網羅方式に変更した。**
手持ちの語彙リスト全部 + **JMdict (EDRDG) の優先度マーカー付き常用語** を採用する。

- 収録: **16,788語** (2文字1,233 / 3文字5,792 / 4文字9,763)
- JMdict取り込み: `tools/import_jmdict.mjs` (優先度 ichi/news/spec/gai 付き読みのみ、カタカナ語はひらがな化)
- 追加除外: 小文字・長音で始まる語 (って/っぽい等の接尾語)
- ライセンス: JMdict由来部分は EDRDG (CC BY-SA)。配布時はREADMEに出典明記。

### 実測KPIと帰結 (measure_kpi.mjs、300試行)
| 指標 | 全網羅辞書 (現行) | 旧コア辞書 (2,396語) |
|---|---|---|
| 配牌テンパイ率 | **66.3%** | 6.0% |
| 平均和了巡目 (全知プレイヤー) | **1巡未満** | 3.6巡 |

**設計上の含意**: 機械的には配牌の2/3が既にテンパイであり、「全部の語を知っている」プレイヤーなら
1〜2巡で決着する。実際の人間は語を自分で見つけられないため体感難易度は語彙力に依存するが、
**ツモ/ロンの自動検出は全辞書で走る**ため、知らない語の組み合わせで即あがりになる局が発生する。
これは全網羅方式の意図的なトレードオフ (認識力 > テンポ)。
テンポを取り戻す調整レバー (未実施・要判断): 自動あがり検出だけ常用コア語に絞る /
手動あがり宣言方式 / あがりにレア牌1枚以上を要求する。

## 難易度設計 (CPU語彙数)
辞書全体が大きくなっても強さが変わらないよう、CPUは**語彙数 (知っている語の数)** で定義する
(`subsetDictByCount`)。旧コア辞書時代の絶対値を引き継いでいる。

| 設定 | CPU語彙数 |
|---|---|
| やさしい | 700語 |
| ふつう | 1,300語 |
| むずかしい | 2,400語 |
| ランク戦 | G=500 〜 S=2,400語 (`CPU_WORDS_BY_RANK`) |

全知プレイヤー基準のトップ率は94〜100% (50試合) — 全網羅方式では人間の語彙力そのものが難易度になる。

---

# 技術構成
- ビルド不要の静的Web + 任意のNodeサーバ。ES Modules。
- `index.html` / `css/style.css`
- `src/engine.js` — コアルール(DOM非依存) / `src/ai.js` — CPU思考
- `src/ui.js` — 画面と進行 / `src/net.js` — WebSocketクライアント
- `src/rank.js` `src/economy.js` `src/collection.js` `src/achievements.js` `src/sound.js` `src/share.js`
- `src/data/words.js` — 辞書 (`tools/build_words.mjs` が `tools/wordlist*.txt` から生成)
- `tools/gameserver.mjs` — 静的配信 + オンライン対戦サーバ
- `tools/build_single.mjs` — 単一HTML化 (`dist/`)

## テスト
```bash
npm test                      # engine / rank / economy / collection / achievements / シミュレーション
node tools/measure_kpi.mjs    # バランスKPIの実測
node tools/validate_words.mjs # 辞書の機械検証
```

---

# バックログ (優先度順)
| 優先 | 項目 |
|---|---|
| 中 | 平均和了巡目を4〜8巡に調整 (語彙の間引き or 和了要件の追加) |
| 中 | 再接続対応 (現状は切断=CPUが引き継ぎ、復帰不可) |
| 中 | ランクRPのサーバ管理 (現状クライアント保存のため改ざん可能) |
| 低 | CSS/JSの責務分割 (画面ごとのファイル分離) |
| 低 | 実機検証の自動化 (320px幅・iOS Safari) |
